<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.petrichor.sincerity.mapper.UserMapper">
    <insert id="addUser" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user (username, password, realname, phone, roleId, avatar, createTime, createBy)
        values(#{username}, #{password}, #{realname}, #{phone}, #{roleId}, #{avatar}, NOW(), #{createBy})
    </insert>
    <select id="getUserById" resultType="com.petrichor.sincerity.entity.User">
        select * from sys_user where id=#{id}
    </select>
    <select id="getUserList"
            parameterType="com.petrichor.sincerity.dto.UserList"
            resultType="com.petrichor.sincerity.dto.UserList"
    >
        select * from sys_user where del_flag=0
        <if test="id!=null">
            AND id = #{id}
        </if>
        <if test="username!= null">
            AND username like concat('%',#{username},'%')
        </if>
    </select>

    <select id="getUserByUserName" resultType="com.petrichor.sincerity.entity.User">
        select * from sys_user where username=#{username} limit 1
    </select>

    <select id="getUserByPhone" resultType="com.petrichor.sincerity.entity.User">
        select * from sys_user where phone=#{phone} limit 1
    </select>

</mapper>